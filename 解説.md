# python-ai-application3 コード解説

このドキュメントは、このプロジェクトのコードが「何をしていて」「どう繋がっているか」を理解するための解説です。

## 1. 全体像

このアプリは **Streamlit** で作った1枚のWebページに、複数のAIライティング機能（記事執筆・メール返信・要約など）を詰め込んだツールです。裏側では **Google Gemini API** にプロンプト（指示文）を送り、返ってきたテキストを画面に表示しています。

データベースや認証は無し。個人のPC上で `.env` に書いたAPIキーを使い、ブラウザ（`localhost:8501`）から操作する構成です。

```
ブラウザ (localhost:8501)
   ↕ Streamlit
app.py  ← サイドバーでツールを選択
   ↕
tools/*.py  ← 選ばれたツールのUIとプロンプト組み立て
   ↕
utils/gemini.py  ← Gemini APIへの唯一の窓口
   ↕
Google Gemini API
```

## 2. ファイル構成と役割

```
app.py                  # エントリポイント。サイドバー＋ツール切り替え
utils/gemini.py         # Gemini APIクライアント（generate関数）
tools/                  # 各ツールの画面とロジック
  blog_writer.py         ブログ記事執筆
  email_reply.py          メール返信文作成
  summarizer.py            文章要約
  proofreader.py           文章校正・リライト
  sns_writer.py            SNS投稿文作成
  catchphrase.py           キャッチコピー・タイトル生成
  translator.py            翻訳
check_api.py             APIキーの動作確認スクリプト（アプリ本体とは別）
.env                     GEMINI_API_KEY を書く場所（Git管理外）
.env.example             .env のひな形
requirements.txt         必要なPythonパッケージ一覧
```

## 3. `app.py` — 全体の司令塔

```python
TOOLS = {
    "📝 ブログ記事執筆": blog_writer.render,
    "📧 メール返信文作成": email_reply.render,
    ...
}
```

ポイントは **辞書に「表示名 → 関数」を登録している**ことです。各ツールファイル（`tools/*.py`）は `render()` という関数を1つ持っていて、それを呼ぶと画面が描画されます。

```python
selected = st.radio("ツール選択", list(TOOLS.keys()), ...)
...
TOOLS[selected]()   # 選ばれたツールのrender()を実行
```

サイドバーの `st.radio` で選んだツール名をキーにして、対応する関数を実行しているだけです。新しいツールを追加したいときは、
1. `tools/新ツール.py` を作って `render()` を実装
2. `app.py` の `TOOLS` 辞書に1行追加

するだけで組み込めます。

### Streamlitの実行モデル（重要な前提知識）

Streamlitは他のWebフレームワークと違い、**ボタンが押されるたびにPythonファイルの中身が上から下まで丸ごと再実行されます**。`if st.button("生成する"):` の中身は、ボタンが押された「その回の再実行」でだけ `True` になり、生成処理が走ります。

サイドバーのモデル選択も同じ仕組みで、選択が変わるたびに `app.py` が再実行され、

```python
os.environ["GEMINI_MODEL"] = selected_model
```

で環境変数を上書きしてから `utils/gemini.py` の `generate()` が呼ばれます。

## 4. `utils/gemini.py` — Gemini API呼び出しの窓口

```python
def _get_client() -> genai.Client:
    api_key = os.getenv("GEMINI_API_KEY")
    ...
    return genai.Client(api_key=api_key)

def generate(prompt: str, model: str = None) -> str:
    if model is None:
        model = os.getenv("GEMINI_MODEL", DEFAULT_MODEL)
    client = _get_client()
    response = client.models.generate_content(model=model, contents=prompt)
    return response.text
```

- `load_dotenv()` が `.env` ファイルを読み込み、`GEMINI_API_KEY` を環境変数として使えるようにしています。
- `generate(prompt)` を呼ぶだけで、「プロンプト文字列を送って、返ってきたテキストを受け取る」処理が完結します。全ツールがこの1関数だけを使っています。
- モデル名は引数で指定しない限り `GEMINI_MODEL` 環境変数（＝サイドバーの選択）を見ます。

**新しいAPIの使い方**：`google.generativeai`（旧パッケージ）ではなく、新しい `google.genai`（パッケージ名は `google-genai`）を使っています。

## 5. `tools/*.py` の共通パターン

どのツールも構造は同じです（`blog_writer.py` を例に）：

```python
def render():
    st.header("...")                     # 見出し
    topic = st.text_input(...)           # 入力フォームを並べる
    length = st.selectbox(...)
    ...

    if st.button("生成する", ...):        # ボタンが押された回だけ実行
        if not topic:
            st.warning("...")            # 未入力チェック
            return

        prompt = f"""..."""               # 入力値からプロンプト文を組み立て

        with st.spinner("生成中..."):
            try:
                result = generate(prompt)  # Gemini APIを呼ぶ
                st.markdown(result)        # 結果を表示
                st.download_button(...)    # ダウンロードボタン
            except Exception as e:
                st.error(f"エラー: {e}")
```

つまり「①フォームで条件を集める → ②プロンプト文字列に埋め込む → ③ `generate()` に投げる → ④結果を表示・ダウンロード可能にする」の4ステップです。

### プロンプトの書き方ルール

全ツール共通で、プロンプトは `【項目名】値` という箇条書き形式で条件を並べ、最後に出力形式を指示する書き方をしています。

```
【テーマ】{topic}
【文字数】{length}
【文体・トーン】{tone}
...
マークダウン形式で出力してください。説明文は不要です。
```

これはGeminiのようなLLMに「何を」「どんな形式で」出してほしいかを明確に伝えるための工夫で、出力のブレを減らすテクニックです。

### ツールごとの個性

| ツール | 仕組み上の特徴 |
|---|---|
| `blog_writer` | 結果をマークダウンとして `st.markdown()` で装飾表示 |
| `email_reply` | 「相手との関係」「言語」などを選択式にして丁寧さを制御 |
| `summarizer` | 元の文字数と要約後の文字数を比較して圧縮率を `st.metric` で表示 |
| `proofreader` | `st.columns(2)` で元の文章と改善後を左右に並べて比較できる |
| `sns_writer` | 選んだSNSの数だけ**ループでAPIを複数回呼ぶ**。同じ `st.text_area` を複数個出すため `key=platform` のように一意なキーを付けないとStreamlitがエラーになる |
| `catchphrase` | 生成結果を改行で分割し、箇条書き（`st.markdown(f"- {line}")`）で表示 |
| `translator` | 翻訳先言語・トーンを選択式にし、逐語訳の併記をチェックボックスで切り替え |

## 6. 環境変数とAPIキーの扱い

- `.env` に `GEMINI_API_KEY=...` を書く（`.gitignore` でGit管理外）
- `python-dotenv` の `load_dotenv()` が起動時に読み込む
- サイドバーからも一時的にAPIキーを入力可能（`os.environ["GEMINI_API_KEY"]` を上書き）。ページを閉じると消える一時的な設定
- `check_api.py` はアプリ本体とは独立したスクリプトで、キーが有効か・どのモデルが使えるかを事前にターミナルで確認するためのもの

## 7. 起動の流れ（まとめ）

```bash
pip install -r requirements.txt   # 依存パッケージをインストール
streamlit run app.py              # Webサーバーを起動
```

1. `app.py` が実行され、Streamlitがローカルサーバー（`localhost:8501`）を立ち上げる
2. ブラウザがサーバーに接続し、サイドバーとツール画面が描画される
3. ユーザーが入力してボタンを押すと `app.py` 全体が再実行され、選択中の `render()` の中の `if st.button(...)` ブロックだけが動く
4. `tools/*.py` がプロンプトを組み立て、`utils/gemini.py` 経由でGemini APIにリクエストを送る
5. 返ってきたテキストが画面に表示され、ダウンロードもできる
